services:
  myochallenge_train:
    container_name: myochallenge_train
    image: ghcr.io/rtae/myochallenge/myochallenge-train:latest
    restart: unless-stopped
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
    shm_size: 30gb
    environment:
      - MUJOCO_GL=osmesa        # 👈 use CPU software renderer
      - OSMESA_FORCE_RATE=1     # optional: stable timing
      - PYOPENGL_PLATFORM=osmesa
    volumes:
      - ./logs:/app/logs
    tty: true

  tensorboard:
    container_name: tensorboard
    image: tensorflow/tensorflow:2.15.0
    command: >
      bash -c "tensorboard --logdir /logs --host 0.0.0.0 --port 6606"
    ports:
      - "6606:6606"
    volumes:
      - ./logs:/logs
    depends_on:
      - myochallenge_train
    restart: unless-stopped
